<% if @user.microposts.any? %>
  <h3>Microposts (<%= @user.microposts.count %>)</h3>
  <%= page_entries_info @microposts_page %>
  <ol class="microposts">
    <%= render @microposts %>
  </ol>
  <%= paginate @microposts_page %>
<% end %>